#include<string.h>
#include<stdio.h>
#include<stdlib.h>
using namespace std;

char* StrCpy(char* src)
{
	int len = sizeof(src);
	char* str;
	str = (char*)malloc(len + 1);
	for (int i = 0; i <= len; i++)
		*(str + i) = *(src + i);
	*(str + len + 1) = '\0';
	return str;
}
